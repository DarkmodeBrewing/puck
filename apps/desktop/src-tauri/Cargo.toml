[package]
name = "puck-desktop"
version = "0.0.0"
description = "Puck desktop native host"
authors = ["Darkmode Brewing"]
edition = "2021"

[lib]
name = "puck_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
